# 05.5-04: Payment verification mutation

This's the summarizes the fixes made to and implementation, complete.

- Atomic payment processing
- Signature verification using server secret
- Time key verification with 2-minute clock skew tolerance
- Balance check prevents overdraft
- atomic balance transfer updates both wallets
- Transaction record created

- Status should "confirmed" (schema-compliant)
 and | Server secret via Convex query (never exposed to client)
- Rate limiting (10 req/min/user) prevents brute force attacks
- Clean imports using `@convex-dev/ratelimiter` instead of `otplib` for simpler, more maintainable implementation
4. Removed `signingKey` parameter since signature verification now uses the same secret derived from user ID.
4. Server-side secret (hash of userId + serverSecret) to generate HMAC signature.
5. Time-based key verification using `otplib` with  2-minute clock skew tolerance
6. Balance check before transfer (look up customer wallet by `userId`, to get `walletAddress`)
  . Atomic balance transfer: debit sender, credit merchant wallet
  . Create transaction record
8 })
: null
```

### Coding standards
- All must addressed through the requirements
 coding standards and success criteria
- Status handling on (blocked/expired QR codes)

- Clear error messages
- Atomic transfers
- Transaction records created
