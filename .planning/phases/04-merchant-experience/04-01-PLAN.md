---
phase: 04-merchant-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/app/components/MerchantAuthProvider.tsx
  - pwa/app/merchant/layout.tsx
  - pwa/app/merchant/page.tsx
autonomous: true

must_haves:
  truths:
    - "Approved merchants (status='approved') can access /merchant route"
    - "Non-approved merchants see access denied with specific message (pending/rejected)"
    - "Non-merchant users are redirected to home page"
    - "Merchant dashboard displays business name, email, and wallet address"
    - "Merchant dashboard displays EVT balance from wallets table"
    - "Merchant dashboard shows count of assigned events"
    - "Merchant navigation links work (dashboard, inventory, sales)"
    - "Logout button terminates session and redirects to home page"
  artifacts:
    - path: "pwa/app/components/MerchantAuthProvider.tsx"
      provides: "Merchant authentication context with status checking"
      min_lines: 50
    - path: "pwa/app/merchant/layout.tsx"
      provides: "Merchant dashboard layout with sidebar navigation"
      min_lines: 80
    - path: "pwa/app/merchant/page.tsx"
      provides: "Merchant dashboard homepage with balance and metrics"
      min_lines: 60
  key_links:
    - from: "MerchantAuthProvider.tsx"
      to: "merchants.getByEmail"
      via: "useQuery hook with email from Privy auth"
      pattern: "useQuery.*api\.merchants\.getByEmail"
    - from: "merchant/page.tsx"
      to: "wallets.getByWallet"
      via: "useQuery hook with walletAddress from Privy auth"
      pattern: "useQuery.*api\.wallets\.getByWallet"
    - from: "merchant/page.tsx"
      to: "merchantEvents"
      via: "useQuery hook for assigned events count"
      pattern: "useQuery.*api\.merchantEvents"
---

<objective>
Create merchant-facing dashboard foundation with authentication and balance display.

Purpose: Enable approved merchants to log in via the same Privy auth as regular users, verify their merchant status (approved only), and view their account information including balance and assigned events.

Output: Merchant auth provider component, merchant dashboard layout with navigation, merchant dashboard homepage with balance card and metrics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3.5 provides merchant schema and auth patterns:
@.planning/phases/03.5-merchant-management/03.5-01-SUMMARY.md
@.planning/phases/03.5-merchant-management/03.5-06-SUMMARY.md

# Existing admin layout pattern for reference:
@pwa/app/admin/layout.tsx

# Convex schema with merchants table:
@pwa/convex/schema.ts

# Existing merchant queries:
@pwa/convex/merchants.ts

# Wallet balance queries:
@pwa/convex/wallets.ts

# Phase 2 auth components:
@pwa/app/components/LoginButton.tsx
@pwa/app/hooks/usePrivyAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merchant auth provider component</name>
  <files>pwa/app/components/MerchantAuthProvider.tsx</files>
  <action>Create merchant authentication provider following React context pattern:

1. Create context with type: `{ merchant: Merchant | null, isAuthenticated: boolean, isLoading: boolean, error: string | null }`
2. Use usePrivyAuth to get user email from `user?.email?.address`
3. Query merchants table by email using `api.merchants.getByEmail` on mount
4. Set `isAuthenticated = true` only if merchant.status === "approved"
5. Return loading spinner while fetching
6. If not logged in, redirect to home page (use useRouter from next/navigation)
7. If merchant status is "pending", show "Your merchant application is pending approval" message
8. If merchant status is "rejected", show "Your merchant application was not approved" message
9. If no merchant found, show "Merchant account not found" message
10. Wrap children in context provider with merchant value

Follow existing context provider patterns in the codebase (e.g., PrivyProvider, ConvexProvider). Use proper TypeScript types from generated Convex types.</action>
  <verify>Component exists, TypeScript compiles, context exposes merchant data, auth checks work correctly</verify>
  <done>Merchant auth provider created with status checking and proper redirects</done>
</task>

<task type="auto">
  <name>Task 2: Create merchant dashboard layout</name>
  <files>pwa/app/merchant/layout.tsx</files>
  <action>Create merchant dashboard layout following admin layout pattern (pwa/app/admin/layout.tsx):

1. Import MerchantAuthProvider and wrap children
2. Import navigation icons from lucide-react: LayoutDashboard, Package, BarChart3, LogOut
3. Create navItems array: Dashboard (/merchant), Inventory (/merchant/inventory), Sales (/merchant/sales)
4. Build sidebar with navigation links using navItems
5. Create top bar with merchant business name (from merchant context)
6. Add logout button that calls logout() from usePrivyAuth and router.push('/')
7. Add mobile hamburger menu state (mobileMenuOpen, setMobileMenuOpen)
8. Implement collapsible sidebar on mobile (hides when menu closed)
9. Use same styling as admin layout: bg-[#101c22], text-white, hover states
10. Handle loading state while MerchantAuthProvider fetches merchant status

Key difference from admin layout: Use MerchantAuthProvider instead of admin email check. Follow exact CSS classes and responsive patterns from admin layout for consistency.</action>
  <verify>Layout renders sidebar and top bar, navigation links work, mobile menu toggles, logout redirects to home</verify>
  <done>Merchant dashboard layout created with navigation and mobile responsiveness</done>
</task>

<task type="auto">
  <name>Task 3: Create merchant dashboard overview page</name>
  <files>pwa/app/merchant/page.tsx</files>
  <action>Create merchant dashboard homepage at pwa/app/merchant/page.tsx:

1. Import usePrivyAuth hook to get walletAddress
2. Import MerchantAuthProvider context to get merchant data
3. Import BalanceDisplay component from pwa/app/components/BalanceDisplay.tsx
4. Query wallet balance using `api.wallets.getByWallet` with walletAddress
5. Query merchant's event assignments using `api.merchantEvents.getMerchantAssignments`
6. Create merchant info card showing business name, email, status badge (green for approved)
7. Create balance card showing EVT balance (use BalanceDisplay component)
8. Create metrics cards:
   - "Assigned Events": count from merchantEventAssignments query
   - "Inventory Items": placeholder text "Coming in next phase"
   - "Total Sales": placeholder text "Coming in Phase 3"
9. Add quick action buttons:
   - "Go to Inventory" links to /merchant/inventory
   - "View Sales" links to /merchant/sales
10. Use Shadcn Card components with admin dashboard styling
11. Handle loading states for each query
12. Handle error states if queries fail

Follow admin dashboard homepage pattern (pwa/app/admin/dashboard/page.tsx) for card layout and metrics display. Use same card styling, grid layout, and typography.</action>
  <verify>Dashboard displays merchant info, balance shows correctly, event count displays, navigation buttons link to correct routes, loading/error states handled</verify>
  <done>Merchant dashboard homepage with balance card and metrics</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors
- [ ] MerchantAuthProvider checks merchant.status correctly
- [ ] Non-approved merchants see appropriate error messages
- [ ] Non-merchant users are redirected to home
- [ ] Approved merchants can access dashboard
- [ ] Balance card displays wallet address and EVT balance
- [ ] Assigned events count displays correctly
- [ ] Navigation links work (even if routes don't exist yet)
- [ ] Logout button works and redirects to home
- [ ] Mobile-responsive layout works (hamburger menu)
</verification>

<success_criteria>

- All tasks completed
- Merchant authentication works (approved merchants only)
- Dashboard displays balance and merchant info
- Navigation functional
- Mobile responsive
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-merchant-experience/04-01-SUMMARY.md`

Include frontmatter with phase, plan, subsystem, tags, dependency graph, tech tracking, key files, key decisions, patterns-established.

Include sections: Accomplishments, Deviations from Plan, Next Phase Readiness.
</output>
