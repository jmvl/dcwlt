---
phase: 04-merchant-experience
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - pwa/app/merchant/inventory/page.tsx
  - pwa/app/components/MerchantInventory.tsx
autonomous: true

must_haves:
  truths:
    - "Merchants can select from their assigned events (via merchantEvents table)"
    - "Merchants see all item groups assigned to them for selected event"
    - "Each item group displays group name, description, and item count"
    - "Item groups are expandable/collapsible (accordion style)"
    - "Each item displays name, description, price (EVT), and stock count"
    - "Per-merchant price overrides display correctly (override vs default price)"
    - "Per-merchant stock overrides display correctly"
    - "Stock badges show correct colors: green (>5), yellow (1-5), red (0)"
    - "Empty state displays when merchant has no assigned events"
    - "Empty state displays when no item groups assigned for selected event"
  artifacts:
    - path: "pwa/app/merchant/inventory/page.tsx"
      provides: "Merchant inventory page with event selector"
      min_lines: 60
    - path: "pwa/app/components/MerchantInventory.tsx"
      provides: "Item groups accordion with merchant override logic"
      min_lines: 100
  key_links:
    - from: "merchant/inventory/page.tsx"
      to: "merchantEvents.getMerchantAssignments"
      via: "useQuery hook with merchantId"
      pattern: "useQuery.*api\.merchantEvents\.getMerchantAssignments"
    - from: "MerchantInventory.tsx"
      to: "itemGroups.getMerchantGroupAssignments"
      via: "useQuery hook with merchantEventId"
      pattern: "useQuery.*api\.itemGroups\.getMerchantGroupAssignments"
    - from: "MerchantInventory.tsx"
      to: "itemGroups.getMerchantItemOverrides"
      via: "useQuery hook for override lookup"
      pattern: "useQuery.*api\.itemGroups\.getMerchantItemOverrides"
---

<objective>
Create merchant inventory view showing assigned item groups and items with per-merchant overrides.

Purpose: Enable merchants to view their assigned item groups and inventory for specific events. This builds on the Phase 3.5 item groups system, displaying merchant-specific pricing and stock overrides where applicable.

Output: Merchant inventory page at /merchant/inventory with event selector, MerchantInventory component showing accordion-style item groups with override logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3.5 provides item groups schema and queries:
@.planning/phases/03.5-merchant-management/03.5-05-SUMMARY.md
@.planning/phases/03.5-merchant-management/03.5-06-SUMMARY.md

# Existing item groups queries:
@pwa/convex/itemGroups.ts

# Admin inventory UI for reference:
@pwa/app/admin/inventory/page.tsx

# Convex schema with item groups tables:
@pwa/convex/schema.ts

# Merchant layout from 04-01:
@.planning/phases/04-merchant-experience/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merchant inventory page with event selector</name>
  <files>pwa/app/merchant/inventory/page.tsx</files>
  <action>Create merchant inventory page at pwa/app/merchant/inventory/page.tsx:

1. Import useMerchantAuth from MerchantAuthProvider context (created in 04-01)
2. Query merchant's event assignments using `api.merchantEvents.getMerchantAssignments` with merchantId
3. Create state for selected event: `selectedEventId` (default to first event if exists)
4. Create event selector dropdown if merchant has multiple events:
   - Use Shadcn Select component or custom dropdown
   - Display event name + date + venue
   - Handle onChange to update selectedEventId state
5. Show "No events assigned" empty state if getMerchantAssignments returns empty
   - Message: "You haven't been assigned to any events yet. Contact your admin."
6. Pass selectedEventId and merchantId to MerchantInventory component
7. Handle loading state while fetching event assignments
8. Handle error state if queries fail

Follow admin inventory page pattern for event selector styling. Use same Tailwind classes and Shadcn components for consistency.</action>
  <verify>Page renders, event selector appears when multiple events exist, empty state shows correctly, selectedEventId passes to child component</verify>
  <done>Merchant inventory page created with event selector</done>
</task>

<task type="auto">
  <name>Task 2: Create item groups accordion component</name>
  <files>pwa/app/components/MerchantInventory.tsx</files>
  <action>Create MerchantInventory accordion component at pwa/app/components/MerchantInventory.tsx:

1. Accept props: eventId (string | Id), merchantId (string | Id)
2. Query merchant's assigned groups using `api.itemGroups.getMerchantGroupAssignments` with merchantEventId
3. Create state for expanded groups: `expandedGroups` (Set or object tracking which groups are expanded)
4. Build accordion-style list:
   - Map through merchantGroupAssignments
   - Each group is clickable (toggles expand/collapse)
   - Show group name, description (optional), and item count
   - Add ChevronDown icon that rotates when expanded (lucide-react)
5. Handle empty state: "No item groups assigned for this event"
6. For each expanded group, query items using `api.itemGroups.getGroupItems` with itemGroupId
7. Display items in expanded section (will be implemented in Task 3)
8. Style using Tailwind: border, padding, hover states, smooth expand/collapse animation
9. Handle loading state while fetching groups

Follow admin inventory page pattern for accordion styling. Use same color scheme and card structure.</action>
  <verify>Component renders, groups display correctly, expand/collapse works, chevron rotates, item count displays accurately</verify>
  <done>Item groups accordion component created</done>
</task>

<task type="auto">
  <name>Task 3: Implement item detail view with merchant overrides</name>
  <files>pwa/app/components/MerchantInventory.tsx</files>
  <action>Add item detail rendering to MerchantInventory component (in expanded group sections):

1. For each expanded group, after querying getGroupItems, map through items
2. For each item, query merchant overrides using `api.itemGroups.getMerchantItemOverrides` with merchantGroupAssignmentId and groupItemId
3. Determine effective price: override.priceOverride if exists, otherwise item.defaultPrice
4. Determine effective stock: override.stockOverride if exists, otherwise item.defaultStock
5. Display item row with:
   - Item name (bold, left-aligned)
   - Description (optional, gray text, truncate if long)
   - Price: formatted to 2 decimal places with " EVT" suffix
   - Stock badge with color coding:
     * Green badge (bg-green-100 text-green-800) if stock > 5 or stock === null
     * Yellow badge (bg-yellow-100 text-yellow-800) if 1 ≤ stock ≤ 5
     * Red badge (bg-red-100 text-red-800) if stock === 0
   - "Price override" small badge (bg-blue-100 text-blue-800) if override exists
   - "Generate QR" button (disabled, will be enabled in 04-03)
6. Format price: `price.toFixed(2)} EVT`
7. Format stock: `stock === null ? "Unlimited" : stock.toString()`
8. Handle loading state while fetching overrides for each item
9. Add "No items in this group" empty state if getGroupItems returns empty

Follow admin inventory item display pattern for styling. Use consistent spacing, typography, and badge styles.</action>
  <verify>Items display with correct data, price/stock overrides work, stock badges have correct colors, override badges appear when applicable</verify>
  <done>Item detail view with merchant overrides implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors
- [ ] Merchants can select from their assigned events
- [ ] Event selector appears when multiple events exist
- [ ] Item groups display for selected event
- [ ] Accordion expand/collapse works smoothly
- [ ] Items display with name, description, price, stock
- [ ] Price overrides show correct values (override vs default)
- [ ] Stock overrides show correct values
- [ ] Stock badges have correct colors (green/yellow/red)
- [ ] Override badges appear when merchant has custom pricing
- [ ] Empty states display when no events or groups assigned
- [ ] Loading states handled for all queries
</verification>

<success_criteria>

- All tasks completed
- Merchants can view assigned item groups and items
- Per-merchant overrides display correctly
- Stock status indicators work correctly
- Empty states handled properly
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-merchant-experience/04-02-SUMMARY.md`

Include frontmatter with phase, plan, subsystem, tags, dependency graph, tech tracking, key files, key decisions, patterns-established.

Include sections: Accomplishments, Deviations from Plan, Next Phase Readiness.
</output>
