---
phase: 04-merchant-experience
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - pwa/app/components/QRCodeGenerator.tsx
  - pwa/app/components/MerchantInventory.tsx
  - pwa/package.json
autonomous: true

must_haves:
  truths:
    - "Solana Pay URL format: solana:<address>?amount=<value>&spl-token=<TOKEN>"
    - "Event Token mint address available in constants from Phase 2"
    - "Merchant wallet address available from merchant record (generated during registration)"
    - "QR code generated client-side using qrcode library"
    - "Modal displays QR code, item name, and price prominently"
    - "Download PNG button saves QR code as image file"
    - "Print button opens browser print dialog for QR code"
    - "Generate QR button in inventory item row opens modal"
  artifacts:
    - path: "pwa/app/components/QRCodeGenerator.tsx"
      provides: "QR code modal with Solana Pay URL generation"
      min_lines: 80
    - path: "pwa/app/components/MerchantInventory.tsx"
      provides: "Inventory with QR generation button integration"
      min_lines: 10
    - path: "pwa/package.json"
      provides: "qrcode dependency for QR code generation"
      min_lines: 1
  key_links:
    - from: "QRCodeGenerator.tsx"
      to: "qrcode library"
      via: "import QRCode from 'qrcode'"
      pattern: "import.*qrcode"
    - from: "QRCodeGenerator.tsx"
      to: "constants.TOKEN_MINT"
      via: "import token mint address for Solana Pay URL"
      pattern: "TOKEN_MINT|EVENT_TOKEN"
    - from: "MerchantInventory.tsx"
      to: "QRCodeGenerator component"
      via: "Import and render modal on QR button click"
      pattern: "QRCodeGenerator.*isOpen"
---

<objective>
Create QR code generation functionality for merchants to accept Solana Pay payments.

Purpose: Enable merchants to generate printable QR codes for each inventory item. Customers can scan these QR codes with Solana Pay-compatible wallets to pay for items using Event Tokens (EVT).

Output: QRCodeGenerator modal component with Solana Pay URL generation, integrated with merchant inventory via "Generate QR" button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 token mint constants:
@event-wallet/src/config/constants.ts

# Existing admin QR generation script (reference):
@pwa/generate-qr.js

# Inventory from 04-02:
@.planning/phases/04-merchant-experience/04-02-PLAN.md

# Solana Pay specification:
# https://solanapay.com/spec/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install QR code generation library</name>
  <files>pwa/package.json</files>
  <action>Install qrcode library and TypeScript types:

1. Navigate to pwa directory: `cd pwa`
2. Run `npm install qrcode @types/qrcode`
3. Verify installation in package.json under dependencies
4. Test import by creating a test line in component file: `import QRCode from 'qrcode'`
5. Remove test line after verifying no TypeScript errors

The qrcode library provides canvas and text-based QR code generation. We'll use the canvas API for downloadable/printable QR codes.</action>
  <verify>package.json includes qrcode and @types/qrcode dependencies, no TypeScript compilation errors after installation</verify>
  <done>QR code library installed with TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create QR code generator component</name>
  <files>pwa/app/components/QRCodeGenerator.tsx</files>
  <action>Create QRCodeGenerator modal component at pwa/app/components/QRCodeGenerator.tsx:

1. Define props interface:
   - isOpen: boolean
   - onClose: () => void
   - merchantAddress: string
   - itemPrice: number
   - itemName: string
   - itemId: string
   - tokenMintAddress: string

2. Build Solana Pay URL:
   - Format: `solana:${merchantAddress}?amount=${itemPrice}&spl-token=${tokenMintAddress}&reference=${itemId}`
   - Use encodeURIComponent for URL parameters if needed

3. Create canvas ref for QR code: `const canvasRef = useRef<HTMLCanvasElement>(null)`

4. Generate QR code using qrcode.toCanvas():
   - Call on mount or when URL changes via useEffect
   - Pass canvas ref and Solana Pay URL
   - Set width to 256px for good scan quality

5. Create modal structure:
   - Backdrop overlay (fixed, inset-0, bg-black/50)
   - Centered modal container (bg-white rounded-lg p-6 max-w-md)
   - Close button (X) in top-right corner
   - Item name (bold, text-xl, mb-2)
   - Price display (text-2xl font-bold, EVT suffix)
   - Canvas element with ref
   - Instructional text: "Scan to pay for {itemName}"

6. Add action buttons:
   - "Download PNG" button: Convert canvas to blob, create download link, trigger click
   - "Print" button: Call window.print() with CSS @media print rules to hide non-QR elements
   - Both buttons use secondary styling (outline or gray background)

7. Handle loading state while QR generates
8. Handle error state if QR generation fails
9. Use Tailwind classes for all styling

Follow Shadcn dialog component pattern for modal structure and animations.</action>
  <verify>Component renders modal when isOpen is true, QR code generates on canvas, Solana Pay URL format is correct, download saves PNG file, print opens dialog, close button dismisses modal</verify>
  <done>QR code generator modal component created</done>
</task>

<task type="auto">
  <name>Task 3: Integrate QR generator into inventory view</name>
  <files>pwa/app/components/MerchantInventory.tsx</files>
  <action>Integrate QRCodeGenerator into MerchantInventory component (from 04-02):

1. Import QRCodeGenerator component at top of file

2. Add state for QR modal:
   - qrModalOpen: boolean (default false)
   - selectedItem: object containing { id, name, price, merchantAddress }

3. Update the "Generate QR" button in each item row (from 04-02 task 3):
   - Change from disabled to enabled
   - Add onClick handler: set selectedItem and set qrModalOpen to true
   - Pass item data to onClick handler

4. Render QRCodeGenerator component:
   - Place at bottom of component return
   - Pass props: isOpen={qrModalOpen}, onClose={() => setQrModalOpen(false)}, merchantAddress, itemPrice, itemName, itemId, tokenMintAddress
   - Use selectedItem state for item-specific props

5. Get token mint address from constants:
   - Import from pwa/src/config/constants.ts or appropriate location
   - Pass to QRCodeGenerator as tokenMintAddress prop

6. Add loading state while QR code generates (optional, handled within QRCodeGenerator)

7. Show error message if QR generation fails (handled within QRCodeGenerator)

8. Ensure modal closes when clicking outside or pressing Escape key (handled within QRCodeGenerator)

Test by clicking "Generate QR" for different items and verifying modal opens with correct item data.</action>
  <verify>"Generate QR" button opens modal for each item, modal displays correct item name and price, modal closes on close button click, different items show different QR codes</verify>
  <done>QR generator integrated into inventory view</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors
- [ ] QR code library installed and imports correctly
- [ ] QRCodeGenerator modal renders when isOpen is true
- [ ] Solana Pay URL format is correct (solana:address?amount=X&spl-token=Y&reference=Z)
- [ ] QR code generates on canvas element
- [ ] Download PNG button saves image file
- [ ] Print button opens browser print dialog
- [ ] Modal displays item name and price correctly
- [ ] "Generate QR" button in inventory opens modal
- [ ] Each item generates unique QR code with correct price
- [ ] Modal closes on close button, backdrop click, or Escape key
- [ ] Loading state shows during QR generation
- [ ] Error state handles QR generation failures
</verification>

<success_criteria>

- All tasks completed
- Merchants can generate Solana Pay QR codes for each inventory item
- QR codes contain correct merchant address, price, and token mint
- QR codes can be downloaded as PNG images
- QR codes can be printed from browser
- Modal shows item name and price clearly
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-merchant-experience/04-03-SUMMARY.md`

Include frontmatter with phase, plan, subsystem, tags, dependency graph, tech tracking, key files, key decisions, patterns-established.

Include sections: Accomplishments, Deviations from Plan, Next Phase Readiness.
</output>
