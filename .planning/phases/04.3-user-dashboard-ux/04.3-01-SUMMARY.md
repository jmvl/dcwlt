---
phase: 04.3-user-dashboard-ux
plan: 01
subsystem: database
tags: [convex, indexes, queries, react-hooks, real-time]

# Dependency graph
requires:
  - phase: 03-topup-payments
    provides: transactions table with merchant indexes
provides:
  - Convex byCustomerByTime index for user transaction lookups
  - listUserTransactions query returning last 5 transactions with item names
  - useUserTransactions React hook for real-time transaction subscription
affects: [user-dashboard-ui, transaction-list-component]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Customer wallet index pattern for user-facing queries
    - Hook-based real-time data subscription with skip pattern

key-files:
  created:
    - pwa/app/hooks/useUserTransactions.ts
  modified:
    - pwa/convex/schema.ts
    - pwa/convex/transactions.ts

key-decisions: []

patterns-established:
  - Pattern: User-side transaction queries mirror merchant queries (byCustomerByTime vs byMerchantByTime)
  - Pattern: Real-time hooks use "skip" token when required params unavailable

# Metrics
duration: 4min
completed: 2026-01-20
---

# Phase 04.3 Plan 1: User Transaction Index and Query Summary

**Convex index and query for user transaction history with real-time React hook integration**

## Performance

- **Duration:** 4 min
- **Started:** 2026-01-20T10:07:21Z
- **Completed:** 2026-01-20T10:11:30Z
- **Tasks:** 3 (3 completed, 1 requires authentication)
- **Files modified:** 2
- **Files created:** 1

## Accomplishments

- Added `byCustomerByTime` index to transactions table for efficient user wallet lookups
- Created `listUserTransactions` query returning last 5 transactions with item names
- Built `useUserTransactions` React hook for real-time transaction subscriptions
- Schema and query compiled successfully, ready for Convex deployment

## Task Commits

Each task was committed atomically:

1. **Task 1: Add byCustomerByTime index to transactions table** - `95e928ccc` (feat)
2. **Task 2: Add listUserTransactions query to transactions.ts** - `865f29a24` (feat)
3. **Task 3: Create useUserTransactions hook** - `cecbf4c64` (feat)

**Plan metadata:** [pending final commit]

## Files Created/Modified

- `pwa/convex/schema.ts` - Added byCustomerByTime index on (customerWallet, timestamp)
- `pwa/convex/transactions.ts` - Added listUserTransactions query with item name joins
- `pwa/app/hooks/useUserTransactions.ts` - Real-time hook for user transaction history

## Decisions Made

None - followed plan as specified. The implementation mirrors the existing merchant transaction query pattern, using the same index structure and item name lookup approach.

## Deviations from Plan

None - plan executed exactly as written.

## Authentication Gates

Task 4 (Deploy Convex schema changes) requires interactive authentication:

**What this means:**
- The schema changes (index) and query code are complete and verified
- To use the new index in production/development, run `cd pwa && npx convex dev`
- This will authenticate with Convex and deploy the schema changes
- Once deployed, the index will be available for queries

**How to complete:**
1. Run `cd pwa && npx convex dev`
2. Follow authentication prompts if needed
3. Confirm index creation in Convex dashboard
4. Test query in dashboard with a wallet address

**Why authentication required:**
Convex deployment requires interactive CLI authentication to push schema changes. This is standard for Convex development workflow and documented in STATE.md under "Blockers/Concerns".

## Issues Encountered

None - all code compiled successfully and follows established patterns.

## User Setup Required

**Convex deployment required:**
To use the new index and query, deploy changes to Convex:
```bash
cd pwa
npx convex dev
```
This will push the schema changes (new index) and query functions to your Convex deployment.

## Next Phase Readiness

**Ready:**
- Index defined in schema, will activate on deployment
- Query function ready to use once deployed
- Hook can be imported in dashboard components immediately
- Follows established patterns from merchant transactions

**Before next phase:**
- Deploy Convex schema changes to enable index
- Test query in Convex dashboard with sample wallet address
- Verify real-time updates work via useUserTransactions hook

**No blockers:** All code is complete and follows established patterns. Deployment is standard workflow step.

---
*Phase: 04.3-user-dashboard-ux*
*Completed: 2026-01-20*
