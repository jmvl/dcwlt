---
phase: 04.4-dashboard-refactor
plan: 02
type: execute
wave: 1
depends_on:
  - 04.4-01
files_modified:
  - pwa/app/dashboard/components/BalanceCard.tsx
  - pwa/app/dashboard/components/ActionButtons.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Balance text displays at 56px (hero size) instead of 36px"
    - "Balance card uses pure CSS gradient without external image"
    - "Balance card padding is 32px for premium feel"
    - "Eye icon button has proper 48px touch target"
    - "Action buttons have no container background"
    - "Action buttons are 64px diameter"
    - "Action buttons have hover and active state animations"
  artifacts:
    - path: "pwa/app/dashboard/components/BalanceCard.tsx"
      provides: "Updated balance display with correct typography and CSS gradient"
      contains: "text-[56px], pure CSS gradient, 32px padding, 48px eye button"
    - path: "pwa/app/dashboard/components/ActionButtons.tsx"
      provides: "Action buttons without container, 64px size, animations"
      contains: "No container div, 64px action-button-circle, hover/active states"
  key_links:
    - from: "BalanceCard.tsx"
      to: "globals.css"
      via: "CSS variables for gradient and spacing"
      pattern: "var\\(--gradient-balance\\)|var\\(--spacing-xl\\)"
    - from: "ActionButtons.tsx"
      to: "globals.css"
      via: "action-button-circle class with 64px size"
      pattern: "action-button-circle"
---

<objective>
Update BalanceCard and ActionButtons components to match design reference specifications.

Purpose: Transform the two most visible dashboard elements (balance card and action buttons) to match design specifications with correct typography, spacing, and visual polish.

Output: Updated components with hero-sized balance text, CSS-only gradient, and polished action buttons
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.4-dashboard-refactor/DESIGN_SYSTEM.md
@.planning/phases/04.4-dashboard-refactor/QUICK_REFERENCE.md
@.planning/research/uix-user-dashboard/code.html
@pwa/app/dashboard/components/BalanceCard.tsx
@pwa/app/dashboard/components/ActionButtons.tsx
@.planning/phases/04.4-dashboard-refactor/04.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update BalanceCard Typography and Gradient</name>
  <files>pwa/app/dashboard/components/BalanceCard.tsx</files>
  <action>
Update BalanceCard.tsx with design specifications:

1. Replace background style (line 26-28):
   - Change from: backgroundImage with url(/images/card-bg.png)
   - Change to: pure CSS gradient: 'linear-gradient(180deg, rgba(0, 188, 212, 0.4) 0%, rgba(10, 18, 41, 0.95) 100%)'
   - Remove url() dependency - CSS only

2. Update padding (line 36):
   - Change from: p-5 (20px)
   - Change to: p-8 (32px)

3. Update "Total Balance" label (line 38):
   - Change from: text-sm (14px)
   - Change to: text-lg (18px)

4. Update balance text (line 39):
   - Change from: text-4xl (36px)
   - Change to: text-[56px] (56px hero size)
   - Add: leading-none

5. Update eye icon button (lines 43-49):
   - Change min-width from: min-w-[56px]
   - Change to: min-w-[48px]
   - Change height from: h-10
   - Change to: h-12
   - Add: active:scale-95 to className
   - Keep existing hover:bg-white/30 and backdrop-blur-md

6. Update icon size (line 48):
   - Change from: w-5 h-5 (20px)
   - Change to: w-6 h-6 (24px)
  </action>
  <verify>
    grep "text-\[56px\]" pwa/app/dashboard/components/BalanceCard.tsx should exist
    grep "p-8" pwa/app/dashboard/components/BalanceCard.tsx should exist
    ! grep "card-bg.png" pwa/app/dashboard/components/BalanceCard.tsx should be true
  </verify>
  <done>
    Balance text is 56px, label is 18px, padding is 32px, gradient is pure CSS, eye button is 48x48px
  </done>
</task>

<task type="auto">
  <name>Update ActionButtons Container and Size</name>
  <files>pwa/app/dashboard/components/ActionButtons.tsx</files>
  <action>
Update ActionButtons.tsx to remove container and increase button size:

1. Remove container div (line 14):
   - Remove: className="flex justify-center items-center gap-12 bg-[#1c2a31]/50 rounded-2xl p-6 border border-white/5"
   - Replace with: className="flex justify-center items-center gap-12 py-6"
   - Remove background, border, and rounded-2xl from container

2. Add hover state to action button circles (lines 20, 31):
   - Add to both: group-hover:scale-105
   - Add to both: transition-transform duration-200
   - Existing group-active:scale-95 stays

3. Update top up link (lines 16-24):
   - Wrap entire link content in div with className="flex flex-col items-center gap-2 cursor-pointer group"
   - Keep existing structure

4. Update cash out button (lines 27-35):
   - Wrap entire button content in div with className="flex flex-col items-center gap-2 cursor-pointer group"
   - Keep existing structure

The action-button-circle class in globals.css is already 64px from plan 01.
  </action>
  <verify>
    ! grep "bg-\[#1c2a31\]" pwa/app/dashboard/components/ActionButtons.tsx should be true
    ! grep "border-white/5" pwa/app/dashboard/components/ActionButtons.tsx should be true
    grep "group-hover:scale-105" pwa/app/dashboard/components/ActionButtons.tsx should exist twice
  </verify>
  <done>
    Action buttons have no container, use 64px size from globals.css, have hover scale animation
  </done>
</task>

</tasks>

<verification>
1. BalanceCard displays balance at 56px (hero size)
2. BalanceCard "Total Balance" label is 18px
3. BalanceCard uses pure CSS gradient (no external image)
4. BalanceCard padding is 32px
5. Eye icon button is 48x48px minimum
6. ActionButtons has no container background or border
7. ActionButtons uses 64px action-button-circle from globals.css
8. ActionButtons have hover scale effect (1.05) and active scale (0.95)
9. Transitions use 200ms duration
</verification>

<success_criteria>
- Balance amount renders at 56px font size
- Balance card uses pure CSS gradient without image dependency
- Balance card interior has 32px padding
- Eye icon button meets 44x44px minimum touch target (48x48px implemented)
- Action buttons float without container background
- Action buttons are 64px diameter
- Hover and active animations are smooth
- No external image dependencies for balance card
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-dashboard-refactor/04.4-02-SUMMARY.md`
</output>
