---
phase: 01-foundation-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pwa/package.json, pwa/tsconfig.json, pwa/next.config.ts, pwa/.gitignore, pwa/.env.local]
autonomous: true

must_haves:
  truths:
    - "Next.js 16 app runs in development mode"
    - "TypeScript compiles without errors"
    - "Tailwind CSS processes styles"
    - "Dev server responds on localhost:3000"
  artifacts:
    - path: "pwa/package.json"
      provides: "Project dependencies and scripts"
      contains: "\"next\": \"^16\""
    - path: "pwa/tsconfig.json"
      provides: "TypeScript configuration"
      contains: "\"strict\": true"
    - path: "pwa/next.config.ts"
      provides: "Next.js configuration"
    - path: "pwa/app/layout.tsx"
      provides: "Root layout with Tailwind"
      min_lines: 15
    - path: "pwa/app/page.tsx"
      provides: "Home page"
      min_lines: 10
  key_links:
    - from: "pwa/app/layout.tsx"
      to: "Tailwind CSS"
      via: "import './globals.css'"
---

<objective>
Create Next.js 16 project with TypeScript and Tailwind CSS as the foundation for the Event Wallet PWA.

Purpose: Establish the base project structure that all subsequent PWA features will build upon. This is the foundation layer - without it, no other work can proceed.

Output: Working Next.js 16 app with TypeScript, Tailwind, and basic home page running on localhost:3000
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

**Phase 1 Requirements (PWA Installation):**
- PWA-01 through PWA-09: PWA installability, manifest, service worker, icons, iOS fallback

**Tech Stack (from PROJECT.md Key Decisions):**
- Next.js 16+ (App Router) - Chosen for PWA support and developer velocity
- TypeScript - For type safety throughout
- Tailwind CSS - For styling (Shadcn/UI dependency)
- Serwist - For service worker/PWA functionality (Plan 01-03)

**Codebase Context:**
- Existing React Native POC (event-wallet/) is REFERENCE ONLY - not modified
- This is a NEW PWA project in `pwa/` directory
- Follow established naming: PascalCase components, camelCase utilities
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Next.js 16 project with App Router</name>
  <files>pwa/package.json, pwa/tsconfig.json, pwa/next.config.ts, pwa/.gitignore</files>
  <action>
Create pwa/ directory and initialize Next.js 16 project:
1. Run: `mkdir -p pwa && cd pwa`
2. Run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --yes`
3. This creates: package.json (Next.js 16), tsconfig.json, next.config.ts, .gitignore, app/ directory structure
4. Verify package.json has Next.js ^16.x
5. Remove auto-generated pages we'll replace: rm app/globals.css (we'll recreate)
6. Create .env.local with empty file for future environment variables
7. Add .gitignore entry: .env.local
  </action>
  <verify>cd pwa && npm run dev starts server on localhost:3000; curl localhost:3000 returns 200</verify>
  <done>Next.js 16 project created, dev server runs, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS with minimal setup</name>
  <files>pwa/app/globals.css, pwa/tailwind.config.ts</files>
  <action>
1. Create app/globals.css with Tailwind directives:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
}
```

2. Verify tailwind.config.ts exists (created by create-next-app)
3. Ensure content paths include: ./app/**/**/*.{ts,tsx}
4. No custom theme yet - Shadcn will extend this in Plan 01-02
  </action>
  <verify>npm run build succeeds; Tailwind classes process (inspect compiled CSS)</verify>
  <done>Tailwind CSS configured and processing styles</done>
</task>

<task type="auto">
  <name>Task 3: Create minimal home page and layout</name>
  <files>pwa/app/layout.tsx, pwa/app/page.tsx</files>
  <action>
1. Modify app/layout.tsx:
```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Event Wallet',
  description: 'Frictionless payments at live events',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

2. Modify app/page.tsx:
```tsx
export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-900">Event Wallet</h1>
        <p className="mt-4 text-lg text-gray-600">Frictionless payments at live events</p>
      </div>
    </main>
  );
}
```
  </action>
  <verify>npm run dev; visit localhost:3000; see centered "Event Wallet" heading and description</verify>
  <done>Home page renders with Tailwind styles</done>
</task>

<task type="auto">
  <name>Task 4: Update package.json scripts for PWA workflow</name>
  <files>pwa/package.json</files>
  <action>
Add to package.json scripts section:
- "build": "next build" (verify exists)
- "start": "next start" (verify exists)
- "lint": "next lint" (verify exists)
- "type-check": "tsc --noEmit" (add for CI)
- Add pwa-specific scripts in later plans (sw-build, etc.)

Verify all scripts exist and are executable.
  </action>
  <verify>npm run type-check passes without errors; npm run build completes</verify>
  <done>All project scripts functional</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts server on localhost:3000
- [ ] TypeScript compiles without errors (npm run type-check)
- [ ] npm run build completes successfully
- [ ] Home page renders at localhost:3000 with correct styles
- [ ] All dependencies in package.json are Next.js 16 compatible
</verification>

<success_criteria>
- Next.js 16 project created in pwa/ directory
- TypeScript and Tailwind CSS configured
- Home page renders with "Event Wallet" branding
- Dev server runs without errors
- Build process completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pwa/01-01-SUMMARY.md` with:
- What was created (project structure, configuration files)
- Commands to run the project (npm run dev, npm run build)
- Next steps (proceed to Plan 01-02: Shadcn/UI integration)
- Any deviations from plan (e.g., if Next.js version differs)
</output>
