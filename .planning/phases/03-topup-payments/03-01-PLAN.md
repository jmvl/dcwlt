---
phase: 03-topup-payments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/app/topup/page.tsx
  - pwa/app/components/TopUpBundle.tsx
  - pwa/convex/topups.ts
  - pwa/convex/wallets.ts
  - pwa/app/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can see predefined token bundles (e.g., $10 = 100 EVT, $20 = 200 EVT)"
    - "User can tap bundle to initiate mock top-up"
    - "Balance updates automatically after top-up completes"
    - "Transaction signature is displayed after top-up"
  artifacts:
    - path: "pwa/app/topup/page.tsx"
      provides: "Top-up bundle selection page"
      min_lines: 30
    - path: "pwa/app/components/TopUpBundle.tsx"
      provides: "Individual bundle card component"
      min_lines: 20
    - path: "pwa/convex/topups.ts"
      provides: "Convex mutation for recording top-ups"
      exports: ["recordTopUp"]
    - path: "pwa/convex/wallets.ts"
      provides: "Mock top-up mutation for balance updates"
      exports: ["mockTopUp"]
  key_links:
    - from: "pwa/app/components/TopUpBundle.tsx"
      to: "pwa/convex/topups.ts"
      via: "useMutation hook calling recordTopUp"
    - from: "pwa/convex/wallets.ts"
      to: "pwa/app/components/BalanceDisplay.tsx"
      via: "Convex real-time subscription"
---

<objective>
Implement mock top-up flow with predefined token bundles.

Purpose: Allow users to add Event Tokens to their wallet without Stripe integration (simulated backend approval).
Output: Top-up page with bundle selection, Convex mutations for recording top-ups, and automatic balance updates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-topup-payments/DISCOVERY.md
@.planning/phases/03-topup-payments/GOLAL_BACKWARD.md

# Prior phase work
@.planning/phases/2-auth-wallet-core-ui/2-04-SUMMARY.md

# Existing components to use
@pwa/app/components/BalanceDisplay.tsx
@pwa/app/components/ConvexProvider.tsx
@pwa/app/hooks/usePrivyAuth.ts
@pwa/convex/wallets.ts
@pwa/convex/users.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create top-up bundle selection page</name>
  <files>pwa/app/topup/page.tsx</files>
  <action>
    Create `/topup` route page that:
    - Uses 'use client' directive for client-side rendering
    - Imports and uses usePrivyAuth hook to check authentication
    - Shows "Please sign in" message if not authenticated
    - Displays 4 predefined bundles in a grid: "$5 = 50 EVT", "$10 = 100 EVT", "$20 = 200 EVT", "$50 = 500 EVT"
    - Each bundle shows price (USD) and token amount clearly
    - Uses design system colors (#101c22 background, #13a4ec primary)
    - Has "Back to Dashboard" link at top
    - Page is responsive (mobile-first)
    
    Use Tailwind for styling. Follow existing dashboard page pattern from pwa/app/dashboard/page.tsx
  </action>
  <verify>Page renders at /topup route, shows 4 bundles, authenticated users see bundles</verify>
  <done>Top-up page accessible, displays all bundle options</done>
</task>

<task type="auto">
  <name>Task 2: Create TopUpBundle component</name>
  <files>pwa/app/components/TopUpBundle.tsx</files>
  <action>
    Create reusable bundle card component that:
    - Accepts props: amount (number), price (number), onPurchase (function)
    - Displays price as "$XX" and tokens as "XX EVT"
    - Uses card styling: dark background (#1a1a1a), border with hover effect
    - "Buy Now" button uses primary color (#13a4ec)
    - Button triggers onPurchase callback with { amount, price }
    - Shows loading state during purchase (spinner or "Processing...")
    - Disabled after purchase completes
    - Responsive sizing (full width on mobile)
    
    Follow existing button and card patterns from BalanceDisplay.tsx
  </action>
  <verify>Component renders without errors, button click fires callback</verify>
  <done>Reusable bundle component with loading states</done>
</task>

<task type="auto">
  <name>Task 3: Create Convex top-up mutations</name>
  <files>pwa/convex/topups.ts, pwa/convex/_generated/api.ts</files>
  <action>
    Create pwa/convex/topups.ts with:
    - Import mutation functions from convex/_generated
    - Define recordTopUp mutation that accepts { walletAddress, amount, price, signature }
    - Mutation stores top-up record in convex schema (for future reference)
    - Returns { success, signature, timestamp }
    
    Also create mockTopUp mutation in pwa/convex/wallets.ts:
    - Accepts { walletAddress, amount }
    - Simulates 2-second delay with Promise
    - Generates mock transaction signature: "mock_${Date.now()}_${Math.random().toString(36).substr(2, 9)}"
    - Calls existing setMockBalance mutation to add amount to current balance
    - Returns { success: true, signature, newBalance }
    
    Update convex/_generated/api.ts stub to include:
    - topups.recordTopUp string identifier
    - wallets.mockTopUp string identifier
  </action>
  <verify>TypeScript compiles, mutations callable from frontend</verify>
  <done>Convex mutations for recording and executing mock top-ups</done>
</task>

<task type="auto">
  <name>Task 4: Integrate top-up flow with UI</name>
  <files>pwa/app/topup/page.tsx</files>
  <action>
    In topup page:
    - Import useMutation from convex/react
    - Import mockTopUp from convex/wallets
    - Get authenticated user's wallet address from usePrivyAuth
    - Pass mockTopUp mutation to each TopUpBundle component
    - Handle purchase success: show success message with transaction signature
    - Handle errors: show error toast/message
    - Add "View Dashboard" button after successful purchase
    - Use React state for loading and success UI
    
    Pattern: Similar to how BalanceDisplay uses useQuery for real-time updates
  </action>
  <verify>Clicking bundle button triggers mock top-up, balance updates, signature shown</verify>
  <done>Complete mock top-up flow working end-to-end</done>
</task>

<task type="auto">
  <name>Task 5: Add top-up navigation link</name>
  <files>pwa/app/dashboard/page.tsx</files>
  <action>
    In dashboard page, add prominent "Top Up" button:
    - Place next to balance display or at top of page
    - Use primary button style (#13a4ec background)
    - Links to /topup route
    - Icon: plus sign or "add" icon (lucide-react Plus icon)
    - Full width on mobile, reasonable width on desktop
  </action>
  <verify>"Top Up" button visible on dashboard, navigates to /topup</verify>
  <done>Easy access to top-up flow from dashboard</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Top-up page accessible at /topup route
- [ ] All 4 bundles display correctly
- [ ] Clicking bundle triggers mock top-up
- [ ] Balance updates in real-time after top-up
- [ ] Transaction signature displayed
- [ ] "Top Up" button present on dashboard
- [ ] Mobile responsive layout works
</verification>

<success_criteria>

- All 5 tasks completed
- Top-up flow works end-to-end (select bundle → balance update → signature shown)
- BalanceDisplay shows updated amount via Convex subscription
- No TypeScript or build errors
- Mobile layout responsive
  </success_criteria>

<output>
After completion, create `.planning/phases/03-topup-payments/03-01-SUMMARY.md`
</output>
