---
phase: 03.5-merchant-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pwa/convex/schema.ts, pwa/convex/merchants.ts, pwa/app/merchant/register/page.tsx]
autonomous: true
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Merchant can submit registration form with email and business info"
    - "Merchant registration creates pending approval state"
    - "Merchant wallet auto-generated via Privy (invisible to merchant)"
    - "Merchant never sees wallet address or private keys"
  artifacts:
    - path: "pwa/convex/schema.ts"
      provides: "Merchants table with email, businessName, status, walletAddress"
      contains: "merchants: defineTable"
    - path: "pwa/convex/merchants.ts"
      provides: "Merchant registration and query functions"
      exports: ["registerMerchant", "getMerchantByEmail", "getPendingMerchants"]
    - path: "pwa/app/merchant/register/page.tsx"
      provides: "Merchant registration form UI"
      min_lines: 50
  key_links:
    - from: "pwa/app/merchant/register/page.tsx"
      to: "pwa/convex/merchants.ts"
      via: "registerMerchant mutation call"
      pattern: "registerMerchant\\("
    - from: "pwa/convex/merchants.ts"
      to: "Privy embedded wallet"
      via: "Privy wallet auto-generation on registration"
      pattern: "privy.*wallet.*embed"
---

<objective>
Implement merchant self-registration flow with email-only signup and automatic Privy embedded wallet generation.

Purpose: Enable merchants to register themselves via web form without seeing wallet complexity. System automatically manages their Solana wallet address via Privy (same pattern as user wallets from Phase 2).

Output: Working merchant registration form, merchants table in Convex, automatic wallet generation invisible to merchant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Prior phase patterns to follow:
@.planning/phases/2-auth-wallet-core-ui/2-01-SUMMARY.md  # Privy embedded wallet pattern
@.planning/phases/2-auth-wallet-core-ui/2-02-SUMMARY.md  # Convex schema patterns

# Current schema to extend:
@pwa/convex/schema.ts
@pwa/convex/wallets.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create merchants table in Convex schema</name>
  <files>pwa/convex/schema.ts</files>
  <action>
    Add merchants table to schema.ts with fields:
    - email: v.string() (primary contact)
    - businessName: v.string() (display name)
    - status: v.string() ("pending" | "approved" | "rejected")
    - walletAddress: v.string() (Privy managed wallet, denormalized for queries)
    - createdAt: v.number()
    - reviewedAt: v.optional(v.number())
    - reviewedBy: v.optional(v.id("users"))
    - notes: v.optional(v.string()) (admin review notes)

    Add indexes:
    - by_email on email field
    - by_status on status field
    - by_wallet on walletAddress field

    Follow pattern from users/wallets tables in Phase 2 (denormalized walletAddress, timestamps).
  </action>
  <verify>npm run build succeeds, Convex schema compiles without errors</verify>
  <done>Merchants table defined with all fields and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Create merchant registration and query functions</name>
  <files>pwa/convex/merchants.ts</files>
  <action>
    Create pwa/convex/merchants.ts with:

    1. registerMerchant mutation:
       - Args: email, businessName
       - Validates email not already registered
       - Creates merchant record with status="pending"
       - Auto-generates Privy embedded wallet (use same pattern as Phase 2 user wallets)
       - Stores walletAddress in merchants table
       - Returns merchant record

    2. getMerchantByEmail query:
       - Args: email
       - Returns merchant record or null

    3. getPendingMerchants query:
       - Returns all merchants with status="pending"
       - Ordered by createdAt (oldest first)

    4. getMerchantByWallet query:
       - Args: walletAddress
       - Returns merchant record or null
       - For payment routing lookup

    Use SSR-safe patterns: validate input, handle errors gracefully.
  </action>
  <verify>TypeScript compiles, Convex functions have proper types</verify>
  <done>Merchants module with registration, email lookup, pending list, wallet lookup</done>
</task>

<task type="auto">
  <name>Task 3: Build merchant registration form page</name>
  <files>pwa/app/merchant/register/page.tsx</files>
  <action>
    Create merchant registration page at pwa/app/merchant/register/page.tsx:

    Form fields:
    - Email (required, email validation)
    - Business Name (required)
    - Business Description (optional, textarea)

    Form behavior:
    - On submit: call registerMerchant mutation
    - Show loading spinner during submission
    - On success: show confirmation message "Application submitted! We'll review your application shortly."
    - On error: show error message (email already exists, validation failed)

    UI requirements:
    - Use Shadcn form components (Card, Input, Button, Label)
    - Clean, professional layout
    - No wallet address display (merchants never see it)
    - Mobile responsive

    Navigation:
    - Add link to merchant registration from home page or footer
  </action>
  <verify>npm run build succeeds, page loads at /merchant/register</verify>
  <done>Merchant registration form with validation, mutation integration, success/error states</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Merchant registration form loads at /merchant/register
- [ ] Form submission creates merchant record in Convex dashboard
- [ ] Merchant record has status="pending" and walletAddress populated
- [ ] Email validation prevents duplicate registrations
- [ ] Privy wallet auto-generation works (check Convex dashboard for walletAddress)
</verification>

<success_criteria>
- Merchants table exists in Convex schema with all required fields
- Merchant registration form accepts email and business info
- Form submission creates merchant record with pending status
- Privy wallet auto-generated and stored in merchants table
- Merchants never see wallet addresses or private keys
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-merchant-management/03.5-01-SUMMARY.md`
</output>
