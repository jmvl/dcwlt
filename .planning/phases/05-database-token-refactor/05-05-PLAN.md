---
phase: 05-database-token-refactor
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/app/api/sponsor-transaction/route.ts (DELETE)
autonomous: true
requirements:
  - DB-TOKEN-07
user_setup: []

must_haves:
  truths:
    - "Gas sponsorship backend endpoint is deleted"
    - "No fee payer wallet configuration needed"
    - "No Solana transaction signing in backend"
  artifacts:
    - path: "pwa/app/api/sponsor-transaction/route.ts"
      provides: "DELETED - no longer exists"
  key_links:
    - from: "N/A (deleted)"
      to: "N/A"
      via: "N/A"
---

<objective>
Delete the gas sponsorship backend API endpoint since we no longer need to sponsor Solana transactions.

Purpose: Remove the /api/sponsor-transaction route that was used for gas sponsorship.
Output: sponsor-transaction directory deleted entirely
</objective>

<execution_context>
@/Users/jm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-database-token-refactor/05-CONTEXT.md
@.planning/phases/05-database-token-refactor/05-RESEARCH.md

<interfaces>
<!-- File to be deleted -->

The file pwa/app/api/sponsor-transaction/route.ts contains:
- POST endpoint that receives partially signed Solana transactions
- Fee payer wallet loading and signing
- Transaction broadcasting to Solana Devnet
- Confirmation polling

All of this is no longer needed since we're using Convex for token balances.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the sponsor-transaction API route</name>
  <files>pwa/app/api/sponsor-transaction/route.ts</files>
  <action>
Delete the entire directory pwa/app/api/sponsor-transaction/ including the route.ts file.

This endpoint was used to:
1. Receive partially signed Solana transactions from the client
2. Sign with fee payer wallet (paying gas fees)
3. Broadcast to Solana network

Since we're now using Convex for token transfers (no blockchain transactions), this entire endpoint is unnecessary.

Command to delete:
```bash
rm -rf pwa/app/api/sponsor-transaction/
```

After deletion, verify the directory no longer exists.
  </action>
  <verify>
    <automated>test ! -d /Users/jm/Codebase/dcwlt/pwa/app/api/sponsor-transaction && echo "Directory deleted successfully" || echo "Directory still exists"</automated>
  </verify>
  <done>sponsor-transaction directory and route.ts file deleted</done>
</task>

</tasks>

<verification>
1. pwa/app/api/sponsor-transaction/ directory does not exist
2. No references to /api/sponsor-transaction in codebase (verified by grep)
</verification>

<success_criteria>
- Gas sponsorship API endpoint completely removed
- No orphaned code referencing this endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-database-token-refactor/05-05-SUMMARY.md`
</output>
