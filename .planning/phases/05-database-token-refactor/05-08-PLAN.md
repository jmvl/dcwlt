---
phase: 05-database-token-refactor
plan: 08
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-06
  - 05-07
files_modified:
  - pwa/.env.local
  - pwa/.env.example
autonomous: true
requirements:
  - DB-TOKEN-07
user_setup: []

must_haves:
  truths:
    - "Feature flag controls token implementation"
    - "Both Solana and database code coexist"
    - "Can switch between implementations via env var"
    - "Application builds and runs in both modes"
  artifacts:
    - path: "pwa/.env.local"
      provides: "Local environment with USE_DATABASE_TOKENS"
    - path: "pwa/.env.example"
      provides: "Documentation for feature flag"
  key_links: []
---

<objective>
Final verification and documentation. Ensure feature flag works correctly and both implementations can be toggled.

Purpose: Verify the refactored system works in both database and Solana modes.
Output: Verified feature flag, updated documentation, ready for production
</objective>

<execution_context>
@/Users/jm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-database-token-refactor/05-CONTEXT.md
@.planning/phases/05-database-token-refactor/05-RESEARCH.md

<interfaces>
<!-- Feature flag verification -->

From pwa/src/config/tokens.ts:
```typescript
export const USE_DATABASE_TOKENS = process.env.NEXT_PUBLIC_USE_DATABASE_TOKENS === 'true';
```

Expected behavior:
- `NEXT_PUBLIC_USE_DATABASE_TOKENS=true` → Database mode (Convex)
- `NEXT_PUBLIC_USE_DATABASE_TOKENS=false` or unset → Solana mode (blockchain)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify feature flag configuration</name>
  <files>pwa/.env.local, pwa/.env.example</files>
  <action>
Verify the feature flag is properly configured:

1. Check pwa/.env.local exists with:
```bash
cat pwa/.env.local | grep USE_DATABASE_TOKENS
```

Should show:
```
NEXT_PUBLIC_USE_DATABASE_TOKENS=true
```

2. Check pwa/.env.example has documentation:
```bash
cat pwa/.env.example | grep -A2 USE_DATABASE_TOKENS
```

Should show:
```
# Token Implementation: 'true' for database, 'false' for Solana
NEXT_PUBLIC_USE_DATABASE_TOKENS=true
```

3. If missing, add to both files.
  </action>
  <verify>
    <automated>cd /Users/jm/Codebase/dcwlt/pwa && grep -c "USE_DATABASE_TOKENS" .env.local .env.example 2>/dev/null || echo "Feature flag not configured"</automated>
  </verify>
  <done>Feature flag configured in both .env.local and .env.example</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds with database mode</name>
  <files>pwa/</files>
  <action>
Verify the application builds successfully with database mode enabled:

```bash
cd pwa
NEXT_PUBLIC_USE_DATABASE_TOKENS=true npm run build
```

Check for any TypeScript errors or build failures.

If build fails, check:
1. All imports are correct
2. No orphaned references to removed code
3. Convex types are generated correctly
  </action>
  <verify>
    <automated>cd /Users/jm/Codebase/dcwlt/pwa && npm run build 2>&1 | tail -20 | grep -E "(error|Error|failed|success|compiled)" || echo "Build status unknown"</automated>
  </verify>
  <done>Build succeeds with USE_DATABASE_TOKENS=true</done>
</task>

<task type="auto">
  <name>Task 3: Document feature flag in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add documentation about the token feature flag to CLAUDE.md:

Add a new section after the architecture section:

```markdown
## Token Implementation Feature Flag

The system supports two token implementations controlled by `NEXT_PUBLIC_USE_DATABASE_TOKENS`:

| Value | Implementation | Use Case |
|-------|---------------|----------|
| `true` | Convex database | Instant payments, no gas, recommended for POC |
| `false` | Solana SPL tokens | Blockchain payments, gas sponsorship required |

**Current default:** Database mode (`true`)

**To switch modes:**
1. Update `pwa/.env.local`
2. Restart the dev server
3. Clear browser cache if needed

**Key files:**
- `pwa/src/config/tokens.ts` - Feature flag definition
- `pwa/app/hooks/usePayment.ts` - Feature-flagged payment logic
- `pwa/app/hooks/useBalance.ts` - Database balance (when flag is true)
- `pwa/app/hooks/useSolanaBalance.ts` - Solana balance (when flag is false)
```
  </action>
  <verify>
    <automated>grep -c "Token Implementation Feature Flag" /Users/jm/Codebase/dcwlt/CLAUDE.md || echo "Documentation not added"</automated>
  </verify>
  <done>Feature flag documented in CLAUDE.md</done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md with phase decisions</name>
  <files>.planning/STATE.md</files>
  <action>
Add the key decisions from this phase to STATE.md.

Add to the Decisions section:

```markdown
**From Plan 05-01:**
139. Feature flag USE_DATABASE_TOKENS controls token implementation - can switch between database and Solana
140. Privy User ID (did:privy:xxx) is primary user identifier instead of wallet address
141. privyId field added to users table with by_privy_id index
142. transferBalance mutation provides atomic balance transfers in database mode

**From Plan 05-06:**
143. usePayment hook supports both implementations via feature flag
144. Database mode: instant Convex mutation, no gas, no signing
145. Solana mode: existing blockchain flow preserved as fallback
146. Both implementations coexist in same codebase

**From Plan 05-08:**
147. Feature flag documented in CLAUDE.md and .env.example
148. Default mode is database (NEXT_PUBLIC_USE_DATABASE_TOKENS=true)
```
  </action>
  <verify>
    <automated>grep -c "Feature flag USE_DATABASE_TOKENS" /Users/jm/Codebase/dcwlt/.planning/STATE.md || echo "Decisions not added"</automated>
  </verify>
  <done>Phase decisions added to STATE.md</done>
</task>

</tasks>

<verification>
1. Feature flag configured in environment files
2. Build succeeds with database mode
3. Documentation added to CLAUDE.md
4. Decisions recorded in STATE.md
</verification>

<success_criteria>
- Feature flag works correctly
- Both implementations can be toggled
- Documentation is complete
- Phase is ready for production
</success_criteria>

<output>
After completion, create `.planning/phases/05-database-token-refactor/05-08-SUMMARY.md`
</output>
