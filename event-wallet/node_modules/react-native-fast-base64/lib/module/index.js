import { NativeModules } from 'react-native';
export function isLoaded() {
  return typeof FastBase64 !== 'undefined';
}
if (!isLoaded()) {
  var _NativeModules$FastBa, _NativeModules$FastBa2;
  (_NativeModules$FastBa = NativeModules.FastBase64) === null || _NativeModules$FastBa === void 0 ? void 0 : (_NativeModules$FastBa2 = _NativeModules$FastBa.install) === null || _NativeModules$FastBa2 === void 0 ? void 0 : _NativeModules$FastBa2.call(_NativeModules$FastBa);
  if (!isLoaded()) {
    throw new Error('JSI bindings were not installed for react-native-fast-base64 module');
  }
}
export function encode(s, urlSafe) {
  return FastBase64.encode(s, urlSafe);
}
export function decode(s) {
  return FastBase64.decode(s);
}
export function fromByteArray(buf, urlSafe) {
  if (buf.buffer.byteLength > buf.byteLength || buf.byteOffset > 0) {
    return FastBase64.fromArrayBuffer(buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength), urlSafe);
  }
  return FastBase64.fromArrayBuffer(buf.buffer, urlSafe);
}
export function toByteArray(s) {
  return new Uint8Array(FastBase64.toArrayBuffer(s));
}
//# sourceMappingURL=index.js.map