import { type IProvider } from "@web3auth/base";
import { EncryptedStorage } from "./types/IEncryptedStorage";
import { SecureStore } from "./types/IExpoSecureStore";
import { ChainConfig, IWeb3Auth, SdkInitParams, SdkLoginParams, State } from "./types/interface";
import { IWebBrowser } from "./types/IWebBrowser";
declare class Web3Auth implements IWeb3Auth {
    ready: boolean;
    private options;
    private webBrowser;
    private keyStore;
    private state;
    private sessionManager;
    private addVersionInUrls;
    private privateKeyProvider;
    private accountAbstractionProvider?;
    constructor(webBrowser: IWebBrowser, storage: SecureStore | EncryptedStorage, options: SdkInitParams);
    get connected(): boolean;
    get provider(): IProvider | null;
    private get baseUrl();
    private get walletSdkUrl();
    set provider(_: IProvider | null);
    init(): Promise<void>;
    login(loginParams: SdkLoginParams): Promise<IProvider | null>;
    logout(): Promise<void>;
    launchWalletServices(chainConfig: ChainConfig, path?: string | null): Promise<void>;
    request(chainConfig: ChainConfig, method: string, params: unknown[], path?: string): Promise<string>;
    enableMFA(): Promise<boolean>;
    userInfo(): State["userInfo"];
    private updateState;
    private createLoginSession;
    private authHandler;
    private authorizeSession;
    private getFinalPrivKey;
    private getFinalCommonPrivKey;
    private getFinalEd25519PrivKey;
}
export default Web3Auth;
